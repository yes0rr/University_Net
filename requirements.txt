# Зависимости проекта University_Net
# Установка:  pip install -r requirements.txt

# Веб-сервер и API
fastapi
uvicorn[standard]

# Чтение таблицы Vuzanet(1).xlsx — нужен только для генератора
# tools/build_tomsk_data.py, для запуска самого сайта не требуется
openpyxl
